---
import { Capitalise } from "@/components/islands/capitalise";
import { Icons } from "@/components/islands/icons";
import { ItbeyCo } from "@/components/islands/itbey-co";
import { ProjectCard } from "@/components/islands/project-card";
import { buttonVariants } from "@/components/islands/ui/button";
import { siteConfig } from "@/config/site";
import { projects } from "@/data/projects";
import { Image } from "astro:assets";
import HomeLayout from "../layouts/HomeLayout.astro";
import RootLayout from "../layouts/RootLayout.astro";
---

<RootLayout title="Portfolio">
	<HomeLayout>
		<section class="space-y-6 pt-6 pb-8 md:pt-10 md:pb-12 lg:py-32">
			<div class="flex max-w-3xl flex-col gap-4">
				<h1 class="font-heading text-4xl font-black md:text-5xl lg:text-[5rem]">
					Hi, I&apos;m Olabode 👋🏽
				</h1>
				<p class="leading-normal text-shadcn-muted-foreground sm:text-xl sm:leading-8">
					I&apos;m a software developer passionate about crafting digital experiences. By day,
					I&apos;m creating value implementing solutions for businesses at <ItbeyCo />, and by night,
					I&apos;m diving into the world of open source (OSS).
				</p>
				<div class="flex flex-wrap gap-4">
					<a href="#about" class={buttonVariants({ size: "lg" })}> Learn More </a>
					<a
						href={siteConfig.links.openSauced}
						target="_blank"
						rel="noreferrer"
						class={buttonVariants({ class: "gap-3", size: "lg", theme: "outline" })}
					>
						<div class="size-2.5 animate-pulse rounded-full border border-red-700/90 bg-red-600">
						</div> See OSS Activity
					</a>
				</div>
			</div>
		</section>

		<section class="space-y-6 pt-6 pb-8 md:pt-10 md:pb-12 lg:py-32">
			<div class="flex flex-col gap-6 lg:flex-row">
				<div class="lg:w-2/5 lg:flex-none">
					<h2 class="font-heading text-4xl font-black tracking-tight md:text-5xl lg:text-[5rem]">
						<Image
							src="/babblebey.png"
							class=`-mt-1 mr-2 inline-block size-10 rounded-full border-2 md:-mt-3 md:mr-4
								md:size-14 lg:-mt-6 lg:size-20`
							alt="Olabode"
							priority={true}
							width={100}
							height={100}
						/>
						<span>I am a dedicated problem solver</span>
					</h2>
				</div>

				<div class="grow">
					<p class="leading-normal text-shadcn-muted-foreground sm:text-xl sm:leading-8">
						I&apos;m not just a software developer; I&apos;m a problem-solver, who live for diving
						into intricate challenges, armed with an unwavering attention to detail and a strategic
						approach to solutions.
					</p>
					<p class="leading-normal text-shadcn-muted-foreground sm:text-xl sm:leading-8">
						Day-to-day, you&apos;ll find me passionately coding away, whether I&apos;m building
						high-conversion website and impactful solutions for businesses over at <ItbeyCo />,
						diving into the heart of open source projects that resonate with my values or even
						bringing to life some of those &apos;dumb ideas&apos; we all have.
					</p>
					<div class="mt-4 flex flex-wrap gap-2">
						{
							Object.keys(siteConfig.links).map((key) => {
								const Icon = Icons[key as keyof typeof Icons];
								const link = siteConfig.links[key as keyof typeof siteConfig.links];
								return (
									<a
										href={link}
										target="_blank"
										class={buttonVariants({ class: "gap-2", theme: "outline" })}
									>
										<Icon className="size-4" /> <Capitalise text={key} client:load />
									</a>
								);
							})
						}
					</div>
				</div>
			</div>
		</section>

		<section class="space-y-6 pt-6 pb-8 md:pt-10 md:pb-12 lg:py-32" id="tools">
			<div class="flex max-w-5xl flex-col gap-6">
				<h2 class="font-heading text-4xl font-black tracking-tight md:text-5xl lg:text-[5rem]">
					Tools & Libraries
				</h2>
				<p class="leading-normal text-shadcn-muted-foreground sm:text-xl sm:leading-8">
					At the heart of my approach to development is problem-solving. I believe in letting the
					challenges guide my choice of tools; PHP and TypeScript are my primary stack. However, my
					journey is peppered with diverse tools—both the ones I&apos;ve honed to mastery and those
					I&apos;ve adeptly wielded; Listed below are few:
				</p>
				<div class="flex flex-wrap gap-5">
					{
						siteConfig.tools.map((tool) => {
							const Icon = Icons[tool.toLowerCase() as keyof typeof Icons];
							return (
								<div class="flex items-center gap-2 p-2 sm:text-lg">
									<Icon
										className={`size-7 ${tool === "NextJS" || tool === "WordPress" ? "dark:invert" : ""}`}
									/>
									{tool}
								</div>
							);
						})
					}
				</div>
			</div>
		</section>

		<section class="space-y-6 pt-6 pb-8 md:pt-10 md:pb-12 lg:py-32" id="projects">
			<div class="flex max-w-5xl flex-col gap-6">
				<h2 class="font-heading text-4xl font-black tracking-tight md:text-5xl lg:text-[5rem]">
					Projects
				</h2>
				<p class="leading-normal text-shadcn-muted-foreground sm:text-xl sm:leading-8">
					I like to engineer solutions that make a difference. Whether it&apos;s crafting
					problem-solving applications for businesses, creating tools that streamline my workflow or
					just hacking around with some ideas, innovation drives me. Here&apos;s a curated project
					collection, ranging from empowering business solutions to personal hacks that have either
					aided my learning or transformed my productivity:
				</p>
				<div class="flex flex-wrap justify-between md:justify-normal md:gap-3">
					{projects.map((p) => <ProjectCard {...p} client:load />)}
				</div>
			</div>
		</section>
	</HomeLayout>
</RootLayout>
